server:
  #启动端口
  port: 9306

spring:
  datasource:
    driver-class-name: org.sqlite.JDBC
    url: jdbc:sqlite::resource:static/sqlite/audioStation.db
    username: web
    password: An87654321!

#swagger api信息
swagger:
  api:
    version: 1.0.0
  contact:
    name: anlinxi
    email: anlinxi@gmail.com


# 配置sql打印日志
mybatis-plus:
  typeAliasesPackage: com.faker.audioStation.mapper
  mapper-locations: classpath*:mapper/*.xml, classpath*:com/gitee/sunchenbin/mybatis/actable/mapping/*/*.xml
  configuration:
    # 嫌日志多可以屏蔽下面这行
    log-impl: org.apache.ibatis.logging.stdout.StdOutImpl


#自定义设置
faker:
  #ffmpeg设置
  ffmpeg:
    #ffmpeg地址设置
    path:
  resources: '/music/'